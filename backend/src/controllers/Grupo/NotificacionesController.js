exports.enviarNotificacion = (usuarioId, mensaje) => {
    // Aquí iría la lógica de notificación (puede ser por email, socket.io, etc.)
    console.log(`Notificación enviada a ${usuarioId}: ${mensaje}`);
};
